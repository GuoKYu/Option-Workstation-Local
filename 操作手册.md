# OptionWorkstation 操作手册

> 版本：0.1.0 ｜ 最后更新：2026-08-24 ｜ 适用环境：Windows 10/11 本地部署
> 仓库：`git@github.com:GuoKYu/Option-Workstation-Local.git`（分支 `main` 本地主枝 / `cloud` 云端分支）

---

## 1. 项目概述

OptionWorkstation 是一个**本地部署**的美股期权分析工作站，用于期权链构建、隐含波动率曲面、希腊字母与套利/风险分析。

- **架构**：Rust（axum）后端 + React 前端，单进程容器。
- **本地访问**：`http://127.0.0.1:7311`（仅本机可达，不暴露到局域网）。
- **OAuth 回调**：`http://localhost:60355/callback`（本地模式，无需在长桥配置白名单）。
- **行情源**：Longbridge OpenAPI（正股用 `SubFlags::QUOTE`，期权用 `SubFlags::DEPTH`）。
- **数据存储**：本地 `./data` 目录（历史回放数据，实时模式不依赖），审计日志 `./.option-workstation/audit.jsonl`。

### 关键路径

| 路径 | 作用 |
|------|------|
| `rust-backend/` | Rust 后端源码（axum 服务、长桥集成、期权分析） |
| `frontend/` | React 前端源码 |
| `rust-backend/target/release/option-workstation.exe` | 编译产物（约 20MB），`start.bat` 直接启动它 |
| `.env` | 本地运行配置 |
| `build-local.bat` | 首次/改动后编译并启动（含工具链检测） |
| `start.bat` | 日常启动（不重新编译，直接跑 exe） |
| `install-mingw-gnu.bat` | 首次安装 MinGW GNU 编译工具链 |

---

## 2. 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11（64 位） |
| 编译工具链 | MinGW-w64 GCC（GNU 工具链），`gcc`/`ld` 位于 `C:\mingw64\bin` |
| Rust | rustup，`stable-x86_64-pc-windows-gnu` 工具链（**必须 GNU，不是 MSVC**） |
| 网络 | 首次编译需从 crates.io 下载依赖（本机实测 GitHub/crates.io 直连常被挡，**手机热点可通**） |
| 长桥账号 | 已注册 Longbridge OpenAPI 账号，并开通对应行情权限（见第 6 节） |

> 说明：本项目**不使用 VS Build Tools / MSVC**，改用 MinGW GNU 工具链，避免微软下载域名被封导致无法安装 `link.exe` 的问题。

---

## 3. 环境准备（仅首次）

### 3.1 安装 MinGW GNU 工具链

双击运行 `install-mingw-gnu.bat`（纯 ASCII 脚本，规避中文乱码）：

1. 自动从 `https://nuwen.net/files/mingw/mingw-20.0.exe` 下载（**非 GitHub、非 Microsoft 源**）。
2. 弹出解压对话框时，解压路径填 `C:\mingw64`。
   - 注意 nuwen 安装包会多套一层 `MinGW/`，若解压后 `C:\mingw64\bin\gcc.exe` 不存在，需手动把 `C:\mingw64\MinGW\*` 移到 `C:\mingw64\` 并删掉空的 `MinGW`。
3. 脚本自动把 `C:\mingw64\bin` 加入系统 PATH，并切换 Rust 到 GNU 工具链。
4. 完成后**开一个新 cmd 窗口**使 PATH 生效。

若 nuwen 下载失败（网络封禁），手动方案：在能联网的设备下载 `mingw-20.0.exe` 拷到本机运行，解压到 `C:\mingw64`，然后手动执行：
```bat
setx PATH "%PATH%;C:\mingw64\bin"
rustup toolchain install stable-x86_64-pc-windows-gnu
rustup default stable-x86_64-pc-windows-gnu
```

### 3.2 安装 Rust（rustup）

从 https://win.rustup.rs/x86_64 下载 rustup-init.exe 安装。安装后确认默认工具链为 GNU：
```bat
rustup default stable-x86_64-pc-windows-gnu
rustc --version     :: 应含 "gnu"
gcc --version       :: 应来自 C:\mingw64\bin
```

### 3.3 获取代码

项目已在本机 `C:\Users\SJTUH\optionworkstation`。如需从 GitHub 重新拉取：
```bat
git clone git@github.com:GuoKYu/Option-Workstation-Local.git
cd Option-Workstation-Local
git checkout main
```
> 推送走 SSH 443 端口（`~/.ssh/config` 已配置 `Host github.com → ssh.github.com:443`），新建空仓库需在 GitHub 网页操作（GitHub MCP 无建仓权限）。

---

## 4. 配置 `.env`

本地模式默认 `.env` 已就位（localhost 模式，无需白名单）。关键项：

```ini
OPTION_WORKSTATION_HOST=127.0.0.1      # 仅本机
OPTION_WORKSTATION_PORT=7311           # Web 端口
OPTION_WORKSTATION_DATA_ROOT=./data
OPTION_WORKSTATION_RISK_FREE_RATE=0.043
OPTION_WORKSTATION_FRONTEND_DIST=./frontend/dist
OPTION_WORKSTATION_AUDIT_PATH=./.option-workstation/audit.jsonl
RUST_LOG=option_workstation=info,tower_http=info
```

> 本地模式**不设** `OW_OAUTH_REDIRECT_BASE`，OAuth 自动用 `http://localhost:60355/callback`。云端部署（cloud 分支）才需要设置该变量（见 `DEPLOY-CLOUD.md` 与 `.env.example`）。

---

## 5. 构建与启动

### 5.1 首次构建 / 代码改动后

双击 `build-local.bat`，它会：

1. 检测 `gcc.exe` 与 `rustup.exe` 是否在 PATH；
2. 激活 `stable-x86_64-pc-windows-gnu` 工具链；
3. 设 `CARGO_HTTP_SSL_NO_REVOKE=true` 与 `RUSTUP_USE_RUSTLS=1`（规避 Windows schannel SSL 吊销检查失败、crates.io 直连问题）；
4. 执行 `cargo build --release --manifest-path rust-backend/Cargo.toml`；
5. 编译成功后直接启动 `option-workstation.exe`。

首次编译约几分钟（下载依赖）。若报 SSL / 网络错误，确认网络可达 crates.io（必要时切手机热点），关闭 cmd 重开后再跑一次。

### 5.2 日常启动

代码未改动时，**双击 `start.bat`** 即可，它直接运行已编译的 `target/release/option-workstation.exe`，不重新编译。

启动后浏览器打开 `http://127.0.0.1:7311`。

> ⚠️ 不要误跑旧脚本 `start-local.bat` / `install-buildtools.bat`（早已废弃：前者逻辑已并入 `build-local.bat`，后者依赖被封的微软域名）。

### 5.3 停止

关闭运行中的 cmd 窗口即可。

---

## 6. 长桥账号与行情权限配置（关键）

OptionWorkstation 走 **OpenAPI 通道**，权限体系与长桥 App / 网页端**相互独立**。务必确认以下两项都已开通：

| 权限 | 渠道 | 必要性 |
|------|------|--------|
| LV1 实时行情（OpenAPI） | OpenAPI | 正股行情（SPY 等）✅ |
| **OPRA 美股期权行情（OpenAPI）** | **OpenAPI** | **期权实时报价（bid/ask/IV）✅ 必开** |
| OPRA 美股期权行情（移动端/桌面端/网页端） | App/Web | 仅 App 内看期权用，**OpenAPI 不认** |

### 开通步骤

1. 登录长桥 **Quote Store（行情商店）**。
2. 找到 **「OPRA 美股期权行情 (OpenAPI)」**（注意带 `(OpenAPI)` 后缀的那一项，不是同名无后缀项），点击开通（推广期免费）。
3. 回到 OptionWorkstation 设置页 → 点 **「更换并验证凭证」** 刷新 OAuth token，使新权限生效。

### 验证权限（可选，用长桥 CLI）

```bat
:: 正股（应有价格返回）
longbridge quote SPY.US --format json

:: 期权链（免费 Level 1，应返回到期日列表）
longbridge option chain SPY.US --format json

:: 单个期权实时报价（需 OPRA OpenAPI 权限）
:: 正确格式：标的+到期(YYMMDD)+C/P+行权价×1000（6 位）
longbridge option quote SPY260918C765000.US --format json
```

- 若最后一条返回 `code=301604: no quote access` → 即**缺 OPRA(OpenAPI) 权限**，按上面步骤开通。
- 期权代码格式：行权价用 6 位（价格×1000，如 765.00 → `765000`），不要多填零。

---

## 7. 常见问题排查

### 7.1 提示「已排队等待 Longbridge 限频窗口；当前 XXX 行情继续推送，约 59 秒后自动重试」

- **出处**：前端 `frontend/src/App.jsx:379`。
- **根因**：后端 `rust-backend/src/live.rs:47` 定义全局期权请求冷却 **`OPTION_REQUEST_COOLDOWN = 65 秒`**（live.rs:214-218）。该冷却是**单个全局时间戳**，所有期权类请求（切到期月订阅 + 后台 90 秒元数据刷新）共享。65 秒内再次请求期权链会被限频排队，到点自动重试。
- **影响**：无害。行情流不受影响，到点自动恢复。
- **规避**：避免在 65 秒内重复点 Start / 频繁切换到期月份；或等它自动重试。
- **（可选代码改进）**：把 `option_request_at` 改为 `HashMap<symbol, Instant>`（按标的分冷却）、或让后台刷新与手动切换用独立时间戳。当前未改，不影响使用。

### 7.2 提示「selected expiration has no usable quotes yet」

- **出处**：后端 `rust-backend/src/live.rs:1319` → 实际由 `analytics.rs:648` 的 `anyhow::ensure!(!rows.is_empty(), "no usable option quotes")` 触发。
- **含义**：选中到期日下的所有合约都构建不出一行有效期权数据（行要成立需算出一个落在 1%~400% 的有效 IV）。
- **最常见根因（本项目实测）**：**缺「OPRA 美股期权行情 (OpenAPI)」权限**。订阅被长桥接受但不推数据 → `cache.depth` 全空 → 无有效 IV → 报此错。
  - 验证：见 6.3 的 CLI 命令，返回 `code=301604` 即确认。
- **次因（瞬态）**：刚切到期月、期权深度行情还没推过来，几秒后自愈。
- **连锁关系**：常与 7.1 同时出现——65s 冷却挡在真正订阅行情之前，导致新到期月没订阅到数据，于是紧接着报「无可用报价」。
- **处理**：
  1. 先确认已开通 **OPRA(OpenAPI)** 权限（见第 6 节），这是根治手段；
  2. 若权限已就绪仍偶发，等几秒让其自动重试；
  3. 个别到期月流动性极差（非主链）可换近月或标准月度到期。

### 7.3 编译报错（SSL / 网络 / 工具链）

| 现象 | 处理 |
|------|------|
| `link.exe not found` | 未用 GNU 工具链。确认 `rustup default stable-x86_64-pc-windows-gnu`，且 `C:\mingw64\bin` 在 PATH。 |
| SSL / `unable to get local issuer certificate` | `build-local.bat` 已设 `CARGO_HTTP_SSL_NO_REVOKE=true`。仍报错则确认网络能达 crates.io（必要时手机热点）。 |
| crates.io 下载超时（直连被挡） | 切手机热点；或检查 `rust-backend/.cargo/config.toml` 应为**空（零自定义源）**，靠默认 crates.io + 环境变量。 |
| `gcc` 找到但 `rustup` 不在 PATH | `build-local.bat` 已显式加 `C:\Users\SJTUH\.cargo\bin`，重开 cmd 后重试。 |
| `sparse url must end with '/'` / `ssl-no-revoke` 键不被识别 | 删除 `config.toml` 里的自定义 source 与 `ssl-no-revoke` 键，仅依赖环境变量。 |

### 7.4 启动后打不开 127.0.0.1:7311

1. 确认 `start.bat` / `build-local.bat` 的 cmd 窗口仍开着（关窗即停服务）。
2. 确认 `target/release/option-workstation.exe` 已生成（未编译成功则没有）。
3. 检查防火墙是否拦了 7311 端口（本机回环一般不需放行）。
4. 看 cmd 窗口日志是否有 panic / 长桥连接错误。

---

## 8. 更新与维护

### 拉取最新代码
```bat
cd C:\Users\SJTUH\optionworkstation
git checkout main
git pull origin main
```

### 改动代码后重新构建
双击 `build-local.bat`（会重新 `cargo build --release` 并启动）。

### 提交与推送
```bat
git add -A
git commit -m "描述改动"
git push origin main          :: 推本地主枝
:: 同步到云端分支
git push origin main:cloud
```
> SSH 走 443（`~/.ssh/config` 已配）。**新建远程仓库需在 GitHub 网页操作**，本机只能对已存在仓库写入。

### 云端分支（cloud）
- `cloud` 分支用于云端部署，使用 `.env.example` / `DEPLOY-CLOUD.md` 中的云端配置（含 `OW_OAUTH_REDIRECT_BASE` 公网回调，需在长桥配置白名单）。本地部署用 `main` 即可。

---

## 9. 文件索引

| 文件 | 作用 |
|------|------|
| `build-local.bat` | 首次/改动后编译+启动（检测工具链、设 SSL 绕过、cargo build、启动 exe） |
| `start.bat` | 日常启动，直接跑已编译 exe，不重编译 |
| `install-mingw-gnu.bat` | 首次安装 MinGW GNU 工具链（nuwen.net 源） |
| `start-local.bat` / `install-buildtools.bat` | **废弃**，勿用 |
| `.env` | 本地运行配置（localhost 模式） |
| `.env.example` / `DEPLOY-CLOUD.md` | 云端部署模板与说明 |
| `rust-backend/.cargo/config.toml` | 应为空（零自定义源），SSL 绕过靠环境变量 |
| `rust-backend/src/live.rs` | 长桥集成、65s 期权冷却、订阅逻辑 |
| `rust-backend/src/analytics.rs` | 期权链构建、IV 计算（`no usable quotes` 判定在 648 行） |
| `frontend/src/App.jsx` | 前端 UI、限频提示（379 行） |

---

## 10. 一句话速查

- **日常用**：双击 `start.bat` → 开 `http://127.0.0.1:7311`。
- **改完代码**：双击 `build-local.bat`。
- **期权没报价**：九成是没开「OPRA 美股期权行情 (OpenAPI)」权限，去行情商店开通后点「更换并验证凭证」。
- **看到限频排队**：无害，等 65 秒自动重试，别在窗口内狂点。
