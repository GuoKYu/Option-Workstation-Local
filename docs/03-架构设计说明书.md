# OptionWorkstation 架构设计说明书

> 文档版本：v1.0 ｜ 编制日期：2026-08-24 ｜ 对应版本：app v0.1.0

---

## 1. 架构总览

OptionWorkstation 采用**单进程单体架构**：Rust（axum）后端同时承担 REST/WebSocket 服务、行情接入、分析计算与静态前端托管；React 单页应用（编译为 `frontend/dist`）由后端直接托管，浏览器访问 `http://127.0.0.1:7311`。

```
┌─────────────────────────────────────────────┐
│ Browser  http://127.0.0.1:7311               │
│   React 19 SPA (echarts / echarts-gl)        │
└───────────────┬─────────────────────────────┘
                │  REST + WebSocket
┌───────────────▼─────────────────────────────┐
│ option-workstation.exe (Rust / axum 0.8)     │
│  ┌───────────────────────────────────────┐  │
│  │ main.rs  Router (/api/*, /api/live/stream)│  │
│  ├────────────┬────────────┬─────────────┤  │
│  │ live.rs    │ analytics.rs│ volatility.rs│  │
│  │ (Longbridge│ (chain/     │ (SVI/SVF     │  │
│  │  OAuth/    │  greeks/IV) │  fit)        │  │
│  │  sub/cool) │             │              │  │
│  ├────────────┼────────────┼─────────────┤  │
│  │ strategy.rs│ replay.rs  │ models.rs    │  │
│  │ (multi-leg)│ (parquet    │ (shared      │  │
│  │            │  replay)   │  types)      │  │
│  ├────────────┴────────────┴─────────────┤  │
│  │ audit.rs (append-only ledger)          │  │
│  └───────────────────────────────────────┘  │
│  外部：Longbridge OpenAPI (CN node)          │
└─────────────────────────────────────────────┘
```

## 2. 技术栈

| 层 | 技术 |
|----|------|
| 后端语言 | Rust（edition 2024） |
| Web 框架 | axum 0.8.9（含 ws） |
| 异步运行时 | tokio 1.47（multi-thread） |
| 行情 SDK | longbridge 4.4.1 |
| 存储/列式 | parquet 59.1（zstd）、arrow-array |
| 统计/数值 | statrs 0.18、rust_decimal 1.39 |
| 缓存 | moka 0.12（sync）、dashmap 6.1 |
| 时间 | chrono 0.4、chrono-tz 0.10、time 0.3 |
| 前端 | React 19、echarts 6、echarts-gl 2、lucide-react、vite 7 |
| 日志 | tracing / tracing-subscriber |

## 3. 模块职责

| 模块 | 职责 |
|------|------|
| `main.rs` | 路由装配、AppState、静态托管、错误处理 |
| `live.rs` | Longbridge 接入：OAuth、订阅（正股 QUOTE / 期权 DEPTH）、65s 期权冷却（`OPTION_REQUEST_COOLDOWN`，live.rs:47）、90s 后台刷新、实时快照、`/api/live/stream` |
| `analytics.rs` | 期权链构建 `build_chain`、IV/希腊字母计算、`no usable quotes` 判定（analytics.rs:648） |
| `volatility.rs` | 波动率上下文、SVI/SVF 微笑与曲面拟合 |
| `strategy.rs` | 多腿策略损益/聚合 Greeks/盈亏平衡分析 |
| `replay.rs` | 历史回放快照（读 Parquet `./data`） |
| `models.rs` | 共享数据模型/类型 |
| `audit.rs` | 审计台账（append-only JSONL）读写 |

## 4. 关键数据流

### 4.1 OAuth 登录（本地模式）
`/api/oauth/start` → 浏览器跳转长桥授权 → 回调 `http://localhost:60355/callback` → `/api/oauth/callback` 换 token → 本地保存 → `/api/oauth/status` 反映状态。

### 4.2 实时快照
`/api/live/session`（建会话/订阅）→ 后台 `live.rs` 拉行情写入 `cache.depth` → `/api/live/snapshot` 出快照 → `/api/live/stream`（WebSocket）持续推送 → 前端节流（2s）更新曲面。

### 4.3 历史回放
前端选交易日/标的 → `/api/v1/replay/snapshot?...` → `replay.rs` 读 Parquet → 返回曲面/波动率上下文 → AbortController 取消过期请求。

### 4.4 策略分析
前端 `StrategyWorkbench` 组装 legs → `POST /api/strategy/analyze` → `strategy.rs` 计算 → 返回损益/聚合 Greeks。

## 5. 关键设计决策

- **本地模式免白名单**：OAuth 回调用 `localhost:60355`，区别于云端 `OW_OAUTH_REDIRECT_BASE`（cloud 分支）。
- **期权请求 65s 全局冷却**：`live.rs:47`，避免触发长桥 301607 限频；单全局时间戳（非按标的分），见风险 R3。
- **OPRA 权限分离**：正股（QUOTE）与期权（DEPTH/OPRA OpenAPI）是两套独立权限，缺期权权限时订阅通过但不推数据。
- **前端节流与取消**：曲面更新 2s 节流（`surfaceUpdateRef`）、回放请求 AbortController，避免抖动。
- **编译工具链 GNU**：因受限网络屏蔽微软域名，采用 MinGW-w64 GCC + `stable-x86_64-pc-windows-gnu`，规避 MSVC `link.exe` 缺失。

## 6. 部署拓扑

- 单机：`127.0.0.1:7311`（Web）+ `localhost:60355`（OAuth 回调）。
- 启动：`start.bat`（直接跑 exe）/ `build-local.bat`（编译+启动）。
- 代码：`main`（本地主枝）/ `cloud`（云端分支）。

## 7. 质量属性

- **可观测**：`RUST_LOG` 分级日志（option_workstation / tower_http）。
- **容错**：限频/无报价提示不阻断行情流；订阅失败回滚（`rollback_option_switch`）。
- **可移植**：零白名单本地模式 + 独立云端配置。
